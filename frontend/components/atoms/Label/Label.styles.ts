import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  label: {
    fontSize: 14,
    fontWeight: '600',
    color: '#495057',
    marginBottom: 8,
  },
  required: {
    color: '#FF6B6B',
  },
  error: {
    color: '#FF6B6B',
  },
});
