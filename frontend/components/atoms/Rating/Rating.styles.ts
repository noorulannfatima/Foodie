import { StyleSheet } from 'react-native';

export const STAR_COLOR = '#FFA94D';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    gap: 2,
  },
  interactive: {
    gap: 0,
  },
  pressed: {
    opacity: 0.6,
  },
});
