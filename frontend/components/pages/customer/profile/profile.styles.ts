// Tab config only — colors/styles come from useCustomerProfileStyles() (global dark mode).

export type TabKey = 'Personal' | 'Business' | 'Settings';
export const TABS: TabKey[] = ['Personal', 'Business', 'Settings'];
