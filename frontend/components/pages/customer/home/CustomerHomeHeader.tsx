import CustomerHeader from '../CustomerHeader';

export default function CustomerHomeHeader() {
  // The greeting section below is white, so the header blends into it.
  return <CustomerHeader background="surface" />;
}
